# Copy to db.properties (gitignored) and fill in, or set env vars DB_URL / DB_USER / DB_PASSWORD instead.
db.url=jdbc:mysql://<host>:<port>/<database>?sslMode=REQUIRED
db.user=<user>
db.password=<password>
